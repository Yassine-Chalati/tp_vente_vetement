specVersion: "v1beta"
serviceId: "vente_vitment"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "vente_vitment-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
